"""Build per-fold LGKE-GNN graph files from processed manifests and feature caches."""

if __name__ == "__main__":
    raise SystemExit("build_graph implementation is pending. See configs/models/m5_lgke_gnn.yaml.")

