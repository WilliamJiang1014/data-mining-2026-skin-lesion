"""Manifest construction entrypoint placeholder.

The final implementation should:
1. validate image files and unique sample identifiers;
2. join metadata, supplemental metadata and labels;
3. remove leakage fields;
4. write `data/processed/manifest_isic.csv` and `manifest_pad.csv`.
"""

